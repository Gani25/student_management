spring.application.name=student_management

# Configuration Related to DB
spring.datasource.url=jdbc:mysql://localhost:3306/sprk_student?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root

# Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Binding Param Use For display values that are passed in ?
logging.level.org.hibernate.orm.jdbc.bind=TRACE
#logging.level.org.hibernate.type.descriptor.sql=TRACE

